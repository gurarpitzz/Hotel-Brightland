import ContactClient from "@/components/ContactClient";

export const metadata = {
  title: "Contact Us | Brightland Hotel, Shimla",
  description: "Contact Brightland Hotel Shimla for room reservations, direct booking best deals on WhatsApp, hotel location directions, and inquiries.",
};

export default function ContactPage() {
  return <ContactClient />;
}

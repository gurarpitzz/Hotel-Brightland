---
deployment:
  tasks:
    - export DEPLOYPATH=$(pwd)
    - /bin/cp -R out/* $DEPLOYPATH/ 2>/dev/null || /bin/cp -R webapp/out/* $DEPLOYPATH/ 2>/dev/null
    - /bin/cp out/.htaccess $DEPLOYPATH/ 2>/dev/null || /bin/cp webapp/out/.htaccess $DEPLOYPATH/ 2>/dev/null
